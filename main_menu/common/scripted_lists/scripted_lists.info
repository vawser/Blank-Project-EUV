scripted_list_key = {
    base = <scriptlist>         #Any type of script list such as "any_country" or "random_character". Imported is the part which comes AFTER the "any_"/"random_"/"every_"/"ordered_"
    conditions = {<triggers>}   #All the triggers applied to elements in the script list
}

#Once a list is created, you can use it like any script list in the game. An example could look like this:
adult_character = {
    base = character
    conditions = { is_adult = yes }
}


#################
every_adult_character = {
    add_adm = -5
}