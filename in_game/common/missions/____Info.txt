#test_mission = { # Mission type
#	header = "" # [String] Image to show in the header when the mission has been selected
#
#	icon = "" # [String] Image to show on the mission selection button and in the header for the active mission
#
#	repeatable = no [Boolean] (Default no) Whether this mission is repeatable
#
#	visible = {} # [Trigger] Whether this mission should be displayed
#
#	enabled = {} # [Trigger] Whether this mission can be completed
#
#	abort = {} # [Trigger] Whether this mission should be aborted
#
#	chance = {} # [script value] Chance of this mission chain appearing. There are POTENTIAL_MISSION_COUNT missions shown at any one time
#
#	ai_will_do = {} # [script value] Chance for ai to pick this mission over other available (contains all scoped variables from the select_triggers)
#
#	on_potential = {} # [Effect] Executed when the mission is added to the list of potential missions for a country. The scope for the mission is created before this effect is executed and persists until after the mission is aborted, completed, or stops being potential
#
#	on_start = {} # [Effect] Executed when the mission is started
#
#	on_abort = {} # [Effect] Executed when the mission is aborted
#
#	on_completion = {} # [Effect] Executed on completion
#
#   on_post_completion = {} # [Effect] Executed after completion when the mission is no longer part of the country
#
#	test_mission_task_2 = { # Task type
#		icon = "" [String] Image to show in the mission for the specific mission task
#
#		requires = { test_mission_task_1 } # [{Task type}] Required tasks for starting
#
#		final = yes # [Boolean] (Default no) Whether completing this task completes the mission
#
#		visible = {} # [Trigger] Whether this task should be displayed
#
#		highlight = {} # [Trigger] Which provinces should be highlighted on the map when mouse is over this task. Province is provided in scope:province
#
#		enabled = {} # [Trigger] Whether the task can be completed
#
#		bypass = {} # [Trigger] Whether the task should be bypassed
#
#		ai_will_do = {} # [script value] Chance for ai to pick this task over other available (contains all scoped variables from the select_triggers)
#
#		duration = 10 # [Days] (Optional) Duration for when this task should complete
#
#		on_monthly = {} # [Effect] Executed each month
#
#		on_start = {} # [Effect] Executed when the timed task is started. Not valid for instant tasks, is not executed if task_rewards_disabled is active in the rules
#
#		on_persistent_start = {} # [Effect] Executed when the timed task is started. Not valid for instant tasks, is ALWAYS executed
#
#		modifier_while_progressing = {} # [Modifiers] Applied to the country while a task is in progress of being completed
#
#		on_completion = {} # [Effect] Executed on completion, is not executed if task_rewards_disabled is active in the rules
#
#		on_persistent_completion = {} # [Effect] Executed on completion, is ALWAYS executed
#
#		on_bypass  = {} # [Effect] Executed on bypass
#	}
#}
