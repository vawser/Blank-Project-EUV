#<religious_aspect_id> = {
#	religion = <religion>	#Can have multiple religion entries, adds the aspect to the religion
#	visible = {			    #Triggers needed to make this religious aspect visible in the first place, scopes into the country trying to use it
#		<triggers>
#	}
#   enabled = {			    #Triggers needed to make this religious aspect available in the first place, scopes into the country trying to use it
#       <triggers>
#   }
#	modifier = {
#		<modifier>			#Country modifiers
#	}
#}